<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 全局参数配置 -->
	<settings>
		<!-- 一级缓存默认开启，但是与spring整合后失效。 -->
		<!-- 开启二级缓存 -->
		<setting name="cacheEnabled" value="true"/>
	</settings>
	
	<!-- 别名 -->
	<typeAliases>
		<!-- 批量定义别名，默认别名为类名(首字母大小写均可) -->
		<!-- <package name="com.xiaol.blog.meta" /> -->

		<!-- 单独定义别名 -->
<!-- 		<typeAlias type="com.xiaol.blog.meta.Blog" alias="Blog"/> -->
	</typeAliases>
</configuration>